// ─── DELETED ─────────────────────────────────────────────────────────────
// This component rendered the full-screen fixed watermark behind page
// content, removed at the user's request. It has no remaining imports
// anywhere in the codebase (already dropped from app/layout.tsx).
// Left as an empty stub only because this sandbox's shell blocks `rm` —
// please delete this file manually.
